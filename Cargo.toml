[package]
name = "ext4-timestomp"
version = "0.1.0"
edition = "2021"

[dependencies]
crc32c = "0.6"
clap = { version = "4", features = ["derive"] }
anyhow = "1"

[profile.release]
lto = true
strip = true
opt-level = "z"
